import {
  Producto,
  DataP,
  ResponseP,
  Query,
  QueryApi,
} from "./producto.interface";
import { Carrito, ResponseC } from "./carrito.interface";

export { Producto, DataP, ResponseP, Carrito, ResponseC, Query, QueryApi };
